<% title = t('views.common.title_new', name: Book.model_name.human.downcase) %>
<% content_for :title, title %>

<h1><%= title %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('views.common.back', name: i18n_pluralize(Book.model_name.human.downcase)), books_path %>
</nav>
