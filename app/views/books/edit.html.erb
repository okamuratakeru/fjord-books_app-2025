<% content_for :title, t('common.edit_page') %>

<h1><%= t('common.edit_page') %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('common.show'), @book %> |
  <%= link_to t('common.back'), books_path %>
</nav>
