<div class="comment-form">
  <%= form_with(model: [commentable, comment]) do |form| %>
    <div>
      <%= form.label :content, "コメント内容", style: "display: block" %>
      <%= form.textarea :content, required: true %>
    </div>
    <div>
      <%= form.submit %>
    </div>
  <% end %>
</div>
